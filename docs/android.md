# Android Support (Legacy)

As of the Rust migration, the active runtime is Tauri + Rust and Capacitor is no longer part of the supported build.

This file is kept only as historical context for old branches/releases that still used Capacitor Android.
